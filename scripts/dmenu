#!/usr/bin/bash

if [ "$HOSTNAME" == "Akatsuki" ]; then
    fontsize=15
    height=26
else
    fontsize=10
    height=20
fi

/usr/bin/dmenu -fn "xos4 terminus -$fontsize" -h $height -b -i \
    -nb '#ebebeb' -nf '#474747' -sb '#0064e4' -sf '#ffffff' $@

