#!/usr/bin/bash

if [ "$HOSTNAME" == "Akatsuki" ]; then
    fontsize=14
    height=24
else
    fontsize=10
    height=20
fi

/usr/bin/dmenu -fn "xos4 terminus -$fontsize" -h $height -b -i -nb '#333333' -nf '#B2B2B2' -sb '#B2B2B2' -sf '#333333' -dim 0 -dc '#333333' $@

